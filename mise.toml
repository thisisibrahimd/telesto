[tools]
go = "1.25.8"
golangci-lint = "2.6.1"
"github:thisisibrahimd/k8s" = { version = "0.0.3-alpha.1", asset_pattern = "k8s-gen_{{ os() | replace(from='macos', to='darwin') }}_{{ arch() | replace(from='x64', to='x86_64') }}.tar.gz" }
