package layouts

import "github.com/thisisibrahimd/telesto/templates/components"

templ Main(title string) {
	<!DOCTYPE html>
	<html>
		<head>
			<title>{ title }</title>
			@components.Head()
		</head>
		<body class="flex">
			@components.Nav()
			{ children... }
		</body>
	</html>
}
